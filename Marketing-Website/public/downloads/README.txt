Place your real APK here as: in-case-of-v1.apk

The download button on the site links to /downloads/in-case-of-v1.apk
Vite serves files in /public at the site root, so dropping the APK in
this folder makes the link work immediately — no code changes needed.
